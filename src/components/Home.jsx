import React from 'react'
import Typography from "@material-ui/core/Typography"

const Home = () => {
    return (
        <main>
            <header component="div">
                <Typography variant="h2" color="primary">Boon</Typography>
                <Typography variant="h4">1. A thing that is helpful or beneficial</Typography>
                <Typography variant="h4">2. A favour or request.</Typography>
            </header>
        </main>
    )
}

export default Home
