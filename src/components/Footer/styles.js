import { useStyles } from "./styles";
import { makeStyles } from "@material-ui/core";

export default makeStyles({
  
  footer: {
    position: "relative",
    bottom: 0,
    textAlign: "center",
    color: "white",
    marginTop: '2rem',

  },
  container:{
    textAlign: "center",
  }
});