import { useStyles } from "./styles";
import { makeStyles } from "@material-ui/core";

export default makeStyles({
  footer: {
    textAlign: "center",
    color: "white",
    marginTop: "2rem",
  },
  container: {
    textAlign: "center",
  },
});
