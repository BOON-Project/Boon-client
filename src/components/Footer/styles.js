import { useStyles } from "./styles";
import { makeStyles } from "@material-ui/core";

export default makeStyles({

  container: {
    textAlign: "center",
  },
  AppBar:{
top: 'none,'
  },
  footer: {
    position: 'relative',
    bottom: '0'
  }

});

